----------------------------------------------------------------------------------

consult('/usr/local/src/FSA/src-compiled-swi/fsa_library').

consult('/usr/local/src/FSA/src-compiled-yap/fsa_library').

use_module(library(lists)).

pl -O -A0 -G0 -L0 -T0 

yap -h 50000 -s 50000 -t 50000

==================================================================================

:- consult('/usr/local/src/fsa6/yap/fsa_library').
:- consult(test).

fsa_regex_compile(file(init),Init),
  fsa_regex_compile(file(trs),TR), 
  go(1000,Init,TR,Result),
  fsa_write_file(test_result,Result).

==================================================================================

fsa_regex_compile(file(reachable),State),
fsa_regex_compile(file('trs-iliv'),TR),
  fsa_regex_compile(minimize(range(compose(fa(State),fa(TR)))),NewState),
  abstr_ft(NewState,AbsState),
  fsa_write_file(yyy,AbsState).


==================================================================================
